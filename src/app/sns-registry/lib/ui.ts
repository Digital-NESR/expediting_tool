/**
 * Panel chrome shared by the record screens.
 *
 * These were defined twice once the attachments panel moved into its own file,
 * which is how two panels on the same page drift apart. One definition.
 */
export const CARD = 'overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm';
export const CARD_HEAD =
  'border-b border-slate-200 bg-slate-50/70 px-4 py-3 text-[13px] font-bold text-slate-800';
export const FIELD_LABEL = 'text-[10.5px] font-bold uppercase tracking-wider text-slate-400';
