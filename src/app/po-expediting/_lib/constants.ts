/* One page of PO groups. Read by the dashboard's slicing, the pagination bar's row-range
   label and the loading skeleton, which is why it does not live in page.tsx any more. */
export const PAGE_SIZE = 50;
